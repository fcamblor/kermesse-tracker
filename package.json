{
  "name": "kt-parent",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "cd ui && npm ci && vite build --outDir ../srv/public/ui/ --base /public/ui/ && cd ../srv && npm ci && npm run build"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
